# Canonical mypy config for the Python SDK — must match sdk-python/pyproject.toml (C-COLO-5).
# The generator lint gate runs `mypy --strict` against the emitted layer; the SDK CI runs the same
# config so the generated layer is type-checked consistently in both contexts.
[mypy]
strict = True
python_version = 3.10
